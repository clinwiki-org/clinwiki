import View from './MailMergeView';
import MailMerge from './MailMerge';
import {
  SchemaType,
  JsonSchema,
} from './SchemaSelector';

export { MailMerge as MailMergeEditor, View as MailMergeView, SchemaType, JsonSchema };
