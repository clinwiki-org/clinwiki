<div class="container-fluid actions row">
  <div class="col-md-3 sidebar">
    <%= render 'shared/study_sidebar' %>
  </div>
  <div class="col-md-8">

    <p><%= @study.brief_title %></p>
    <%= form_for(@annotation) do |f| %>
      <% if @annotation.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@annotation.errors.count, "error") %> prohibited this annotation from being saved:</h2>

          <ul>
          <% @annotation.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <%= f.hidden_field :nct_id, :value => @annotation.nct_id %>
      <%= f.label :label %>
      <%= f.text_field :label, class: 'form-control' %>

      <%= f.label :description %>
      <%= f.text_area :description, size: "30x10", class: 'form-control' %>

      <p><%= f.submit "Save", class: "btn btn-primary" %></p>
    <% end %>

  </div>
</div>
