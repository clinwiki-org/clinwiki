// import { fromJS } from 'immutable';
// import { makeSelectLoginSignupDomain } from '../selectors';

// const selector = makeSelectLoginSignupDomain();

describe('makeSelectLoginSignupDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
